import type { Metadata } from "next";
import GameCard from "@/components/games/GameCard";
import { gameCategories, globalGames } from "@/data/games";
export const metadata:Metadata={title:"Free Online Games – Board, Puzzle, Brain & Casual | Mindrailo",description:"Play free browser games including chess, Sudoku, Connect Four, Snake, number puzzles and family classics.",alternates:{canonical:"/games/"}};
export default function GamesPage(){return <main><section className="arcade-hero"><div className="page-container"><p className="eyebrow">🎮 Global Games Arcade</p><h1>Pick a game. Play instantly.</h1><p>Free browser games for global players—no account, no download and no hunting through menus.</p><div className="arcade-stat-row"><span><strong>{globalGames.length}</strong> launch games</span><span><strong>0</strong> downloads</span><span><strong>100%</strong> browser play</span></div></div></section><section className="section"><div className="page-container"><div className="section-heading"><div><span className="small-label">Start here</span><h2>Most fun games</h2></div></div><div className="arcade-grid">{globalGames.filter(g=>g.featured).map(g=><GameCard key={g.slug} game={g}/>)}</div></div></section>{gameCategories.map(category=>{const games=globalGames.filter(g=>g.category===category);if(!games.length)return null;return <section className="section section-soft" key={category}><div className="page-container"><div className="section-heading"><div><span className="small-label">Explore</span><h2>{category} games</h2></div></div><div className="arcade-grid">{games.map(g=><GameCard key={g.slug} game={g}/>)}</div></div></section>})}</main>}
